//count how many entries have the target value (2)
#include <iostream>

using namespace std;

int twos = 0;
int const target = 2;

for (size_t i = 0; i < v.size(); i++)
  {
    if(v[i] == target)
      {
	twos++;
      }
    
  }

