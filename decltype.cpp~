using namespace std;

int main()
{
  int i = 10;
  cout << typeid(decltype(i + 1.0)).name() << endl; //outputs "double"

  vector<int> a;
  decltype(a) b;
  b.push_back(10);
  decltype(a)::iterator iter = a.end();
  
}
