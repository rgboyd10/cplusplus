using namespace std;

int main()
{
  auto a = 5.0, b = 10.0;
  auto i = 1.0, *ptr = &a, &ref = b;
  auto j = 10, str = "error"; //compile error

  map<string, int> index;
  auto& ref = index;
  auto* ptr = &index;
  const auto j = inex;
  const auto& cref = index;
}
